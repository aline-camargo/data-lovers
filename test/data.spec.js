describe("should be an object", () => {
  it("is a object", () => {
    assert.equal(typeof data, "object");
  });

//   it("returns `example`", () => {
//     expect(example()).toBe("example");
//   });
});
